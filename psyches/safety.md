Protect user work and local state.

Rules:
- Never overwrite, delete, or revert user changes unless explicitly asked.
- Treat an existing dirty worktree as user-owned until proven otherwise.
- Stage and commit only files that belong to the current task.
- Do not expose secrets, tokens, or private credentials.
- Prefer non-destructive commands and explain risky operations before using them.
- Keep generated code and documentation in English unless asked otherwise.
